name: Chat-Auto

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: teste
      run: cd /var/www/

    - name: teste 2
      run: npm init
